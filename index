<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Cabeça Barber Shoper – Estilo que transforma</title>
    <link href="https://fonts.googleapis.com/css2?family=Oswald:wght@400;600;700&family=Montserrat:wght@400;600;700;900&family=Playfair+Display:ital,wght@0,700;1,700&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
    <style>
        /* ===== RESET & BASE ===== */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --gold: #c9a84c;
            --gold-light: #e8d28a;
            --dark: #0c0b0a;
            --dark-gray: #1a1716;
            --medium: #2a2725;
            --light: #f5f0eb;
            --red-accent: #b33a2a;
            --shadow-gold: 0 0 40px rgba(201, 168, 76, 0.3);
            --shadow-red: 0 0 40px rgba(179, 58, 42, 0.25);
        }

        html {
            scroll-behavior: smooth;
        }

        body {
            font-family: 'Montserrat', sans-serif;
            background: var(--dark);
            color: var(--light);
            overflow-x: hidden;
        }

        /* ===== HERO COM IMAGEM DE FUNDO ===== */
        .hero {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 140px 20px 80px;
            position: relative;
            z-index: 1;
            background-image: url('https://i.im.ge/QMWTVZy/File.jpg');
            background-size: cover;
            background-position: center top;
            background-repeat: no-repeat;
        }

        .hero::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(12, 11, 10, 0.65);
            z-index: -1;
        }

        .hero-content {
            text-align: center;
            max-width: 950px;
            position: relative;
            z-index: 2;
        }

        .hero-badge {
            display: inline-block;
            padding: 6px 22px;
            border: 1px solid rgba(201, 168, 76, 0.25);
            border-radius: 50px;
            font-size: 0.8rem;
            letter-spacing: 3px;
            text-transform: uppercase;
            color: var(--gold);
            background: rgba(201, 168, 76, 0.05);
            margin-bottom: 25px;
        }
        .hero-badge i {
            margin-right: 8px;
        }

        /* TÍTULO PRINCIPAL EM 3D */
        .hero-title {
            font-family: 'Playfair Display', serif;
            font-size: clamp(3.5rem, 14vw, 8rem);
            font-weight: 700;
            line-height: 1;
            margin-bottom: 15px;
            text-shadow: 
                0 2px 0 rgba(201, 168, 76, 0.3),
                0 4px 0 rgba(201, 168, 76, 0.2),
                0 8px 12px rgba(0,0,0,0.6),
                0 12px 20px rgba(0,0,0,0.4);
            transform: perspective(800px) rotateX(2deg) rotateY(-1deg);
            transition: transform 0.4s ease;
        }
        .hero-title:hover {
            transform: perspective(800px) rotateX(0deg) rotateY(0deg) scale(1.02);
        }
        .hero-title .gold {
            color: var(--gold);
            text-shadow: 
                0 2px 0 #a8893a,
                0 4px 0 #8a6f2e,
                0 8px 12px rgba(0,0,0,0.6),
                0 12px 20px rgba(0,0,0,0.4);
        }
        .hero-title .light {
            color: #fff;
            text-shadow: 
                0 2px 0 #ccc,
                0 4px 0 #999,
                0 8px 12px rgba(0,0,0,0.6),
                0 12px 20px rgba(0,0,0,0.4);
        }

        .hero-sub {
            font-size: clamp(1.2rem, 2.8vw, 1.8rem);
            color: #ccc;
            font-weight: 300;
            margin-bottom: 12px;
            text-shadow: 0 4px 12px rgba(0,0,0,0.8);
        }
        .hero-sub strong {
            color: #fff;
            font-weight: 700;
        }

        .hero-desc {
            font-size: 1.1rem;
            color: #ddd;
            max-width: 600px;
            margin: 18px auto 35px;
            line-height: 1.8;
            text-shadow: 0 2px 8px rgba(0, 0, 0, 0.8);
        }

        .hero-buttons {
            display: flex;
            flex-wrap: wrap;
            gap: 18px;
            justify-content: center;
            align-items: center;
        }

        /* BOTÃO PRINCIPAL - WHATSAPP EM DESTAQUE */
        .btn-primary {
            display: inline-flex;
            align-items: center;
            gap: 16px;
            padding: 22px 56px;
            background: #25d366;
            border: none;
            border-radius: 60px;
            color: #fff;
            font-size: 1.4rem;
            font-weight: 700;
            text-decoration: none;
            box-shadow: 0 10px 40px rgba(37, 211, 102, 0.6);
            transition: 0.4s;
            cursor: pointer;
            font-family: 'Montserrat', sans-serif;
            text-transform: uppercase;
            letter-spacing: 1px;
            position: relative;
            animation: pulseWppBtn 2s ease-in-out infinite;
            transform: perspective(400px) rotateX(0deg);
        }
        .btn-primary i {
            font-size: 2.2rem;
        }
        .btn-primary:hover {
            transform: perspective(400px) rotateX(2deg) scale(1.06) translateY(-5px);
            box-shadow: 0 0 80px rgba(37, 211, 102, 0.8);
        }

        @keyframes pulseWppBtn {
            0%, 100% { box-shadow: 0 10px 40px rgba(37, 211, 102, 0.5); }
            50% { box-shadow: 0 10px 70px rgba(37, 211, 102, 0.9); }
        }

        .btn-secondary {
            display: inline-flex;
            align-items: center;
            gap: 12px;
            padding: 18px 48px;
            background: transparent;
            border: 2px solid rgba(255, 255, 255, 0.12);
            border-radius: 60px;
            color: #fff;
            font-size: 1.1rem;
            font-weight: 600;
            text-decoration: none;
            transition: 0.4s;
            text-transform: uppercase;
            letter-spacing: 1px;
            transform: perspective(400px) rotateX(0deg);
        }
        .btn-secondary:hover {
            border-color: var(--gold);
            box-shadow: 0 0 40px rgba(201, 168, 76, 0.15);
            transform: perspective(400px) rotateX(2deg) translateY(-4px);
        }

        .hero-stats {
            display: flex;
            justify-content: center;
            gap: 50px;
            margin-top: 55px;
            padding-top: 40px;
            border-top: 1px solid rgba(255, 255, 255, 0.05);
        }
        .stat-item {
            text-align: center;
        }
        .stat-number {
            font-family: 'Oswald', sans-serif;
            font-size: 2.4rem;
            font-weight: 700;
            color: var(--gold);
            text-shadow: 0 0 20px rgba(201,168,76,0.3);
        }
        .stat-label {
            font-size: 0.8rem;
            color: #ccc;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-top: 4px;
        }

        .badge-whatsapp {
            display: inline-block;
            background: rgba(37, 211, 102, 0.15);
            border: 1px solid rgba(37, 211, 102, 0.3);
            border-radius: 50px;
            padding: 6px 24px;
            font-size: 0.8rem;
            font-weight: 600;
            color: #25d366;
            letter-spacing: 1px;
            text-transform: uppercase;
            margin-bottom: 20px;
        }
        .badge-whatsapp i {
            margin-right: 8px;
        }

        /* ===== HEADER ===== */
        .header {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            padding: 18px 40px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            z-index: 100;
            background: rgba(12, 11, 10, 0.8);
            backdrop-filter: blur(14px);
            border-bottom: 1px solid rgba(201, 168, 76, 0.15);
        }

        .logo {
            font-family: 'Playfair Display', serif;
            font-size: 2.2rem;
            font-weight: 700;
            color: var(--gold);
            text-shadow: var(--shadow-gold);
            letter-spacing: 2px;
            transform: perspective(400px) rotateX(0deg);
            transition: transform 0.3s ease;
        }
        .logo:hover {
            transform: perspective(400px) rotateX(4deg) scale(1.02);
        }
        .logo span {
            color: var(--light);
            font-weight: 400;
        }

        .nav-links {
            display: flex;
            gap: 32px;
            list-style: none;
            align-items: center;
        }

        .nav-links a {
            color: #bbb;
            text-decoration: none;
            font-weight: 600;
            font-size: 0.9rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            transition: 0.3s;
            position: relative;
        }

        .nav-links a::after {
            content: '';
            position: absolute;
            bottom: -4px;
            left: 0;
            width: 0;
            height: 2px;
            background: var(--gold);
            transition: 0.3s;
        }

        .nav-links a:hover {
            color: #fff;
        }
        .nav-links a:hover::after {
            width: 100%;
        }

        .nav-cta {
            padding: 10px 28px;
            background: var(--gold);
            border-radius: 40px;
            color: var(--dark) !important;
            font-weight: 700;
            box-shadow: var(--shadow-gold);
            transition: 0.3s;
        }
        .nav-cta:hover {
            transform: scale(1.05);
            box-shadow: 0 0 60px rgba(201, 168, 76, 0.5);
        }
        .nav-cta::after {
            display: none !important;
        }

        /* ===== SERVICES ===== */
        .services {
            padding: 100px 20px;
            background: rgba(12, 11, 10, 0.6);
            backdrop-filter: blur(4px);
        }

        .section-title {
            text-align: center;
            font-family: 'Playfair Display', serif;
            font-size: clamp(2.2rem, 4.5vw, 3.8rem);
            margin-bottom: 12px;
            text-shadow: 0 4px 12px rgba(0,0,0,0.5);
        }
        .section-title .gold {
            color: var(--gold);
            text-shadow: 0 0 30px rgba(201,168,76,0.2);
        }
        .section-sub {
            text-align: center;
            color: #aaa;
            max-width: 600px;
            margin: 0 auto 55px;
            font-size: 1.05rem;
        }

        .services-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
            gap: 30px;
            max-width: 1200px;
            margin: 0 auto;
        }

        .service-card {
            background: rgba(255, 255, 255, 0.02);
            border: 1px solid rgba(201, 168, 76, 0.08);
            border-radius: 24px;
            padding: 40px 30px;
            text-align: center;
            transition: 0.4s;
            position: relative;
            overflow: hidden;
            backdrop-filter: blur(4px);
            transform: perspective(600px) rotateX(0deg) rotateY(0deg);
            transition: all 0.4s ease;
        }
        .service-card:hover {
            transform: perspective(600px) rotateX(3deg) rotateY(2deg) translateY(-8px);
            border-color: rgba(201, 168, 76, 0.25);
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5);
        }
        .service-card::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: conic-gradient(from 0deg, transparent, rgba(201, 168, 76, 0.04), transparent, rgba(179, 58, 42, 0.04), transparent);
            animation: rotateSlow 18s linear infinite;
            opacity: 0;
            transition: 0.6s;
        }
        .service-card:hover::before {
            opacity: 1;
        }
        @keyframes rotateSlow {
            100% { transform: rotate(360deg); }
        }

        .service-card .icon {
            font-size: 3.2rem;
            margin-bottom: 18px;
            position: relative;
            z-index: 1;
        }
        .service-card h3 {
            font-family: 'Oswald', sans-serif;
            font-size: 1.5rem;
            margin-bottom: 8px;
            color: #fff;
            position: relative;
            z-index: 1;
        }
        .service-card p {
            color: #aaa;
            font-size: 0.95rem;
            position: relative;
            z-index: 1;
        }
        .service-card .price {
            font-size: 1.8rem;
            font-weight: 900;
            color: var(--gold);
            margin-top: 14px;
            position: relative;
            z-index: 1;
        }
        .service-card .old-price {
            font-size: 1rem;
            color: #666;
            text-decoration: line-through;
            margin-right: 8px;
        }
        .service-card .badge-premium {
            display: inline-block;
            background: var(--red-accent);
            color: #fff;
            font-size: 0.7rem;
            font-weight: 700;
            padding: 2px 14px;
            border-radius: 20px;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-top: 8px;
        }

        /* ===== SCHEDULE ===== */
        .schedule {
            padding: 100px 20px;
            background: rgba(12, 11, 10, 0.9);
        }

        .schedule-container {
            max-width: 600px;
            margin: 0 auto;
            background: rgba(255, 255, 255, 0.02);
            border: 1px solid rgba(201, 168, 76, 0.1);
            border-radius: 32px;
            padding: 50px 40px;
            backdrop-filter: blur(12px);
            box-shadow: 0 30px 80px rgba(0, 0, 0, 0.6);
            transform: perspective(600px) rotateX(0deg);
            transition: transform 0.4s ease;
        }
        .schedule-container:hover {
            transform: perspective(600px) rotateX(2deg);
        }

        .form-group {
            margin-bottom: 24px;
        }
        .form-group label {
            display: block;
            font-family: 'Oswald', sans-serif;
            font-size: 0.95rem;
            color: #ccc;
            margin-bottom: 6px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        .form-group label i {
            color: var(--gold);
            margin-right: 8px;
        }
        .form-group input,
        .form-group select {
            width: 100%;
            padding: 15px 20px;
            background: rgba(255, 255, 255, 0.04);
            border: 1px solid rgba(255, 255, 255, 0.06);
            border-radius: 16px;
            color: #fff;
            font-size: 1rem;
            font-family: 'Montserrat', sans-serif;
            transition: 0.3s;
        }
        .form-group input:focus,
        .form-group select:focus {
            outline: none;
            border-color: var(--gold);
            box-shadow: 0 0 30px rgba(201, 168, 76, 0.1);
            background: rgba(255, 255, 255, 0.06);
        }
        .form-group select option {
            background: #1a1716;
            color: #fff;
        }

        .submit-btn {
            width: 100%;
            padding: 20px;
            background: var(--gold);
            border: none;
            border-radius: 60px;
            color: var(--dark);
            font-size: 1.1rem;
            font-weight: 700;
            font-family: 'Oswald', sans-serif;
            cursor: pointer;
            transition: 0.4s;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 12px;
            box-shadow: var(--shadow-gold);
            text-transform: uppercase;
            letter-spacing: 1px;
            transform: perspective(400px) rotateX(0deg);
        }
        .submit-btn:hover {
            transform: perspective(400px) rotateX(2deg) scale(1.02);
            box-shadow: 0 0 80px rgba(201, 168, 76, 0.5);
        }

        /* ===== PHRASES ===== */
        .phrases {
            padding: 80px 20px;
            background: rgba(12, 11, 10, 0.4);
        }

        .phrases-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 25px;
            max-width: 1000px;
            margin: 0 auto;
        }

        .phrase-card {
            background: rgba(255, 255, 255, 0.02);
            border-left: 4px solid var(--gold);
            padding: 24px 28px;
            border-radius: 0 16px 16px 0;
            transition: 0.3s;
            transform: perspective(400px) rotateX(0deg);
        }
        .phrase-card:hover {
            transform: perspective(400px) rotateX(2deg) translateX(8px);
            background: rgba(201, 168, 76, 0.04);
        }
        .phrase-card .emoji {
            font-size: 1.8rem;
            margin-bottom: 6px;
        }
        .phrase-card p {
            font-style: italic;
            color: #ddd;
            font-size: 1rem;
            line-height: 1.6;
        }
        .phrase-card .author {
            margin-top: 10px;
            font-size: 0.85rem;
            color: var(--gold);
            font-weight: 600;
        }

        /* ===== BENEFITS ===== */
        .benefits {
            padding: 80px 20px;
            background: rgba(12, 11, 10, 0.7);
        }

        .benefits-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(170px, 1fr));
            gap: 30px;
            max-width: 1000px;
            margin: 0 auto;
            text-align: center;
        }

        .benefit-item {
            padding: 30px 15px;
            transition: 0.3s;
            border-radius: 20px;
            border: 1px solid transparent;
            transform: perspective(400px) rotateX(0deg);
        }
        .benefit-item:hover {
            transform: perspective(400px) rotateX(3deg) scale(1.02);
            border-color: rgba(201, 168, 76, 0.1);
            background: rgba(255, 255, 255, 0.02);
        }
        .benefit-item i {
            font-size: 2.6rem;
            color: var(--gold);
            margin-bottom: 14px;
        }
        .benefit-item h4 {
            font-family: 'Oswald', sans-serif;
            font-size: 1.1rem;
            color: #fff;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        /* ===== PROMO SECTION ===== */
        .promo {
            padding: 80px 20px;
            background: radial-gradient(ellipse at 50% 80%, rgba(201, 168, 76, 0.06) 0%, transparent 60%), var(--dark);
        }

        .promo-box {
            max-width: 900px;
            margin: 0 auto;
            background: linear-gradient(145deg, rgba(201, 168, 76, 0.05), rgba(26, 23, 22, 0.8));
            border: 1px solid rgba(201, 168, 76, 0.15);
            border-radius: 40px;
            padding: 50px 40px;
            text-align: center;
            position: relative;
            overflow: hidden;
            transform: perspective(600px) rotateX(0deg);
            transition: transform 0.4s ease;
        }
        .promo-box:hover {
            transform: perspective(600px) rotateX(2deg) scale(1.01);
        }
        .promo-box::before {
            content: '★';
            position: absolute;
            top: -30px;
            right: -20px;
            font-size: 12rem;
            color: rgba(201, 168, 76, 0.04);
        }
        .promo-box .tag {
            display: inline-block;
            background: var(--red-accent);
            color: #fff;
            padding: 4px 24px;
            border-radius: 30px;
            font-size: 0.8rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 2px;
            margin-bottom: 18px;
        }
        .promo-box h2 {
            font-family: 'Playfair Display', serif;
            font-size: clamp(2rem, 4vw, 3.2rem);
            color: #fff;
        }
        .promo-box h2 .gold {
            color: var(--gold);
        }
        .promo-box p {
            color: #bbb;
            font-size: 1.1rem;
            max-width: 500px;
            margin: 15px auto 25px;
        }
        .promo-box .price-big {
            font-size: 3rem;
            font-weight: 900;
            color: var(--gold);
            font-family: 'Oswald', sans-serif;
        }
        .promo-box .price-big small {
            font-size: 1.2rem;
            font-weight: 400;
            color: #888;
            text-decoration: line-through;
            margin-right: 12px;
        }

        /* ===== FOOTER ===== */
        .footer {
            padding: 60px 20px 30px;
            text-align: center;
            border-top: 1px solid rgba(255, 255, 255, 0.04);
        }
        .footer h2 {
            font-family: 'Playfair Display', serif;
            font-size: clamp(1.8rem, 3.5vw, 2.8rem);
            margin-bottom: 12px;
        }
        .footer h2 .gold {
            color: var(--gold);
        }
        .footer p {
            color: #999;
            font-size: 1.05rem;
            margin-bottom: 22px;
        }
        .phone-number {
            font-size: 2rem;
            font-weight: 700;
            font-family: 'Oswald', sans-serif;
            color: var(--gold);
            margin: 12px 0 22px;
        }
        .footer-copy {
            color: #444;
            font-size: 0.8rem;
            margin-top: 40px;
        }

        /* ===== WHATSAPP FLOAT ===== */
        .float-whatsapp {
            position: fixed;
            bottom: 28px;
            right: 28px;
            width: 66px;
            height: 66px;
            background: #25d366;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2.2rem;
            color: #fff;
            text-decoration: none;
            box-shadow: 0 10px 40px rgba(37, 211, 102, 0.5);
            z-index: 1000;
            animation: pulseWpp 2.4s ease-in-out infinite;
            transition: 0.3s;
            transform: perspective(400px) rotateX(0deg);
        }
        .float-whatsapp:hover {
            transform: perspective(400px) rotateX(6deg) scale(1.08);
        }
        @keyframes pulseWpp {
            0%, 100% { box-shadow: 0 10px 30px rgba(37, 211, 102, 0.4); }
            50% { box-shadow: 0 10px 60px rgba(37, 211, 102, 0.8); }
        }

        /* ===== MODAL ===== */
        .modal-overlay {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.85);
            backdrop-filter: blur(8px);
            z-index: 2000;
            align-items: center;
            justify-content: center;
        }
        .modal-overlay.active {
            display: flex;
        }
        .modal {
            background: linear-gradient(145deg, #1a1716, #0c0b0a);
            border: 1px solid rgba(201, 168, 76, 0.15);
            border-radius: 32px;
            padding: 50px 40px;
            text-align: center;
            max-width: 420px;
            width: 90%;
            animation: modalPop 0.5s ease;
            box-shadow: 0 40px 100px rgba(0, 0, 0, 0.8);
            transform: perspective(600px) rotateX(0deg);
        }
        @keyframes modalPop {
            0% { transform: scale(0.6) rotate(-3deg); opacity: 0; }
            100% { transform: scale(1) rotate(0); opacity: 1; }
        }
        .modal .icon-big {
            font-size: 4rem;
            margin-bottom: 10px;
        }
        .modal h3 {
            font-family: 'Playfair Display', serif;
            font-size: 2rem;
            color: var(--gold);
            margin: 14px 0;
        }
        .modal p {
            color: #aaa;
            margin-bottom: 24px;
            line-height: 1.6;
        }
        .modal-close {
            padding: 14px 44px;
            background: var(--gold);
            border: none;
            border-radius: 60px;
            color: var(--dark);
            font-size: 1rem;
            font-weight: 700;
            cursor: pointer;
            font-family: 'Montserrat', sans-serif;
            transition: 0.3s;
            box-shadow: var(--shadow-gold);
        }
        .modal-close:hover {
            transform: scale(1.05);
        }

        /* ===== RESPONSIVE ===== */
        @media (max-width: 768px) {
            .header {
                padding: 14px 18px;
                flex-direction: column;
                gap: 12px;
            }
            .nav-links {
                gap: 14px;
                flex-wrap: wrap;
                justify-content: center;
            }
            .nav-cta {
                padding: 6px 18px;
                font-size: 0.75rem;
            }
            .hero {
                padding-top: 180px;
                background-position: center center;
            }
            .hero-stats {
                flex-direction: column;
                gap: 18px;
            }
            .schedule-container {
                padding: 28px 18px;
            }
            .btn-primary {
                padding: 18px 34px;
                font-size: 1.2rem;
            }
            .btn-primary i {
                font-size: 1.8rem;
            }
            .btn-secondary {
                padding: 14px 30px;
                font-size: 0.95rem;
            }
            .promo-box {
                padding: 30px 20px;
            }
        }

        /* ===== SCROLL ANIMATION ===== */
        .fade-in {
            opacity: 0;
            transform: translateY(40px);
            transition: all 0.8s ease;
        }
        .fade-in.visible {
            opacity: 1;
            transform: translateY(0);
        }
    </style>
</head>

<body>

    <!-- ===== WHATSAPP FLOAT ===== -->
    <a href="https://wa.me/5513996286050?text=Olá!%20Quero%20agendar%20meu%20corte%20na%20Cabe%C3%A7a%20Barber%20Shoper!%20✂️"
    class="float-whatsapp" target="_blank" title="Agende pelo WhatsApp">
    <i class="fab fa-whatsapp"></i>
</a>

<!-- ===== HEADER ===== -->
<header class="header">
    <div class="logo">Cabeça<span>Barber</span></div>
    <ul class="nav-links">
        <li><a href="#servicos">Serviços</a></li>
        <li><a href="#agendar">Agendar</a></li>
        <li><a href="#depoimentos">Depoimentos</a></li>
        <li><a href="#promo" class="nav-cta">Promoção</a></li>
    </ul>
</header>

<!-- ===== HERO (TOPO, TELA INTEIRA) ===== -->
<section class="hero" id="home">
    <div class="hero-content">
        <div class="badge-whatsapp">
            <i class="fab fa-whatsapp"></i> Agendamento via WhatsApp
        </div>
        <div class="hero-badge">
            <i class="fas fa-star"></i> Estilo que transforma
        </div>
        <h1 class="hero-title">
            <span class="gold">Cabeça</span><br />
            <span class="light">Barber Shoper</span>
        </h1>
        <p class="hero-sub">
            <strong>Mais que um corte</strong> · uma atitude que fala por você
        </p>
        <p class="hero-desc">
            Cada detalhe pensado para realçar sua melhor versão. Técnica,
            precisão e um toque de excelência que fazem toda diferença.
        </p>
        <div class="hero-buttons">
            <a href="https://wa.me/5513996286050?text=Olá!%20Quero%20agendar%20meu%20corte!%20✂️"
            class="btn-primary" target="_blank">
            <i class="fab fa-whatsapp"></i> Agendar agora
        </a>
        <a href="#servicos" class="btn-secondary">
            <i class="fas fa-arrow-right"></i> Ver serviços
        </a>
    </div>
    <div class="hero-stats">
        <div class="stat-item">
            <div class="stat-number">+450</div>
            <div class="stat-label">Clientes satisfeitos</div>
        </div>
        <div class="stat-item">
            <div class="stat-number">4.9</div>
            <div class="stat-label">Avaliação média</div>
        </div>
        <div class="stat-item">
            <div class="stat-number">8</div>
            <div class="stat-label">Anos de experiência</div>
        </div>
    </div>
</div>
</section>

<!-- ===== SERVICES ===== -->
<section class="services" id="servicos">
    <h2 class="section-title">Nossos <span class="gold">Serviços</span></h2>
    <p class="section-sub">Escolha o tratamento ideal e eleve seu visual a um novo patamar.</p>
    <div class="services-grid">
        <div class="service-card fade-in">
            <div class="icon">✂️</div>
            <h3>Corte Executivo</h3>
            <p>Design sob medida com acabamento impecável.</p>
            <div class="price">R$ 35</div>
        </div>
        <div class="service-card fade-in">
            <div class="icon">💈</div>
            <h3>Degradê Premium</h3>
            <p>Transição perfeita com efeito de profundidade.</p>
            <div class="price">R$ 40</div>
        </div>
        <div class="service-card fade-in">
            <div class="icon">🧔</div>
            <h3>Barba Autoridade</h3>
            <p>Modelagem e alinhamento com toque de mestre.</p>
            <div class="price">R$ 25</div>
        </div>
        <div class="service-card fade-in">
            <div class="icon">👁️</div>
            <h3>Sobrancelha</h3>
            <p>Design simétrico para realçar seu olhar.</p>
            <div class="price">R$ 15</div>
        </div>
        <div class="service-card fade-in">
            <div class="icon">✨</div>
            <h3>Acabamento Ouro</h3>
            <p>Detalhes premium com brilho e textura.</p>
            <div class="price">R$ 20</div>
        </div>
        <div class="service-card fade-in">
            <div class="icon">👑</div>
            <h3>Combo Cabeça</h3>
            <p>Corte + Barba + Sobrancelha + Acabamento.</p>
            <div class="price"><span class="old-price">R$ 95</span> R$ 80</div>
            <span class="badge-premium">⭐ Premium</span>
        </div>
    </div>
</section>

<!-- ===== PROMO SECTION ===== -->
<section class="promo" id="promo">
    <div class="promo-box fade-in">
        <span class="tag"><i class="fas fa-bolt"></i> Oferta especial</span>
        <h2>Pacote <span class="gold">Executivo</span></h2>
        <p>O combo completo para quem exige o melhor. Inclui corte, barba, sobrancelha e acabamento premium.</p>
        <div class="price-big">
            <small>R$ 120</small> R$ 89
        </div>
        <br /><br />
        <a href="https://wa.me/5513996286050?text=Quero%20o%20Pacote%20Executivo!%20👑"
        class="btn-primary" target="_blank">
        <i class="fab fa-whatsapp"></i> Garantir minha vaga
    </a>
</div>
</section>

<!-- ===== SCHEDULE ===== -->
<section class="schedule" id="agendar">
    <h2 class="section-title">Agende <span class="gold">seu horário</span></h2>
    <p class="section-sub">Preencha os dados e confirme pelo WhatsApp. Sua transformação começa aqui.</p>
    <div class="schedule-container fade-in">
        <form id="scheduleForm">
            <div class="form-group">
                <label><i class="fas fa-user"></i> Seu nome</label>
                <input type="text" id="clientName" placeholder="Digite seu nome completo" required />
            </div>
            <div class="form-group">
                <label><i class="fas fa-phone"></i> Telefone</label>
                <input type="tel" id="clientPhone" placeholder="(13) 99999-9999" required />
            </div>
            <div class="form-group">
                <label><i class="fas fa-cut"></i> Serviço desejado</label>
                <select id="serviceType" required>
                    <option value="">Selecione</option>
                    <option value="Corte Executivo">✂️ Corte Executivo</option>
                    <option value="Degradê Premium">💈 Degradê Premium</option>
                    <option value="Barba Autoridade">🧔 Barba Autoridade</option>
                    <option value="Sobrancelha">👁️ Sobrancelha</option>
                    <option value="Acabamento Ouro">✨ Acabamento Ouro</option>
                    <option value="Combo Cabeça">👑 Combo Cabeça</option>
                    <option value="Pacote Executivo">⭐ Pacote Executivo</option>
                </select>
            </div>
            <div class="form-group">
                <label><i class="fas fa-calendar"></i> Data</label>
                <input type="date" id="scheduleDate" required />
            </div>
            <div class="form-group">
                <label><i class="fas fa-clock"></i> Horário</label>
                <select id="scheduleTime" required>
                    <option value="">Selecione</option>
                    <option value="09:00">09:00</option>
                    <option value="09:30">09:30</option>
                    <option value="10:00">10:00</option>
                    <option value="10:30">10:30</option>
                    <option value="11:00">11:00</option>
                    <option value="11:30">11:30</option>
                    <option value="13:00">13:00</option>
                    <option value="13:30">13:30</option>
                    <option value="14:00">14:00</option>
                    <option value="14:30">14:30</option>
                    <option value="15:00">15:00</option>
                    <option value="15:30">15:30</option>
                    <option value="16:00">16:00</option>
                    <option value="16:30">16:30</option>
                    <option value="17:00">17:00</option>
                    <option value="17:30">17:30</option>
                    <option value="18:00">18:00</option>
                    <option value="18:30">18:30</option>
                    <option value="19:00">19:00</option>
                </select>
            </div>
            <button type="submit" class="submit-btn">
                <i class="fab fa-whatsapp"></i> Confirmar agendamento
            </button>
        </form>
    </div>
</section>

<!-- ===== PHRASES ===== -->
<section class="phrases" id="depoimentos">
    <h2 class="section-title">O que dizem <span class="gold">por aí</span></h2>
    <p class="section-sub">Frases que capturam a essência da Cabeça Barber Shoper.</p>
    <div class="phrases-grid">
        <div class="phrase-card fade-in">
            <div class="emoji">🔥</div>
            <p>"Cada visita é uma experiência única. Saio daqui com autoestima renovada e visual impecável."</p>
            <div class="author">– Rafael S.</div>
        </div>
        <div class="phrase-card fade-in">
            <div class="emoji">👔</div>
            <p>"Atendimento top, ambiente sofisticado e resultados que impressionam. Recomendo a todos."</p>
            <div class="author">– Marcelo P.</div>
        </div>
        <div class="phrase-card fade-in">
            <div class="emoji">💪</div>
            <p>"O corte perfeito existe e está aqui. Profissionalismo e qualidade em cada detalhe."</p>
            <div class="author">– Fernando L.</div>
        </div>
        <div class="phrase-card fade-in">
            <div class="emoji">⭐</div>
            <p>"Melhor barbearia da região. O combo premium é surreal de bom, vale cada centavo."</p>
            <div class="author">– Bruno M.</div>
        </div>
        <div class="phrase-card fade-in">
            <div class="emoji">🏆</div>
            <p>"Visual de respeito, atendimento de primeira. Já indiquei para toda a galera."</p>
            <div class="author">– Thiago R.</div>
        </div>
        <div class="phrase-card fade-in">
            <div class="emoji">✨</div>
            <p>"Aqui não é só um corte, é uma transformação. Saio me sentindo outro homem."</p>
            <div class="author">– Gustavo C.</div>
        </div>
    </div>
</section>

<!-- ===== BENEFITS ===== -->
<section class="benefits" id="beneficios">
    <h2 class="section-title">Por que <span class="gold">escolher</span> a gente?</h2>
    <div class="benefits-grid">
        <div class="benefit-item fade-in">
            <i class="fas fa-paint-brush"></i>
            <h4>Design exclusivo</h4>
        </div>
        <div class="benefit-item fade-in">
            <i class="fas fa-star"></i>
            <h4>Produtos premium</h4>
        </div>
        <div class="benefit-item fade-in">
            <i class="fas fa-crown"></i>
            <h4>Atendimento VIP</h4>
        </div>
        <div class="benefit-item fade-in">
            <i class="fas fa-smile"></i>
            <h4>Resultado garantido</h4>
        </div>
        <div class="benefit-item fade-in">
            <i class="fas fa-bolt"></i>
            <h4>Transformação imediata</h4>
        </div>
    </div>
</section>

<!-- ===== FOOTER ===== -->
<footer class="footer" id="contato">
    <h2>Vem viver o <span class="gold">Cabeça</span></h2>
    <p>Agende seu horário e descubra um novo patamar de estilo e confiança.</p>
    <div class="phone-number">
        <i class="fas fa-phone"></i> (13) 99628-6050
    </div>
    <a href="https://wa.me/5513996286050?text=Olá!%20Quero%20agendar%20meu%20corte!%20✂️"
    class="btn-primary" target="_blank">
    <i class="fab fa-whatsapp"></i> Falar com a gente
</a>
<p class="footer-copy">© 2025 Cabeça Barber Shoper · Todos os direitos reservados</p>
</footer>

<!-- ===== MODAL SUCCESS ===== -->
<div class="modal-overlay" id="successModal">
    <div class="modal">
        <div class="icon-big">✅</div>
        <h3>Agendamento enviado!</h3>
        <p>Seu pedido foi direcionado ao nosso WhatsApp. Em breve confirmamos seu horário.</p>
        <button class="modal-close" onclick="closeModal()">Fechar</button>
    </div>
</div>

<!-- ===== SCRIPTS ===== -->
<script>
    // ===== SCROLL ANIMATIONS =====
    const fadeEls = document.querySelectorAll('.fade-in');
    const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                entry.target.classList.add('visible');
            }
        });
    }, { threshold: 0.15 });

    fadeEls.forEach(el => observer.observe(el));

    // ===== MASK TELEFONE =====
    document.getElementById('clientPhone').addEventListener('input', function(e) {
        let value = e.target.value.replace(/\D/g, '');
        if (value.length <= 11) {
            if (value.length > 7) {
                value = `(${value.slice(0,2)}) ${value.slice(2,7)}-${value.slice(7)}`;
            } else if (value.length > 2) {
                value = `(${value.slice(0,2)}) ${value.slice(2)}`;
            } else if (value.length > 0) {
                value = `(${value}`;
            }
        }
        e.target.value = value;
    });

    // ===== DATE MIN =====
    document.getElementById('scheduleDate').setAttribute('min', new Date().toISOString().split('T')[0]);

    // ===== FORM SUBMIT =====
    document.getElementById('scheduleForm').addEventListener('submit', function(e) {
        e.preventDefault();

        const name = document.getElementById('clientName').value.trim();
        const phone = document.getElementById('clientPhone').value.trim();
        const service = document.getElementById('serviceType').value;
        const date = document.getElementById('scheduleDate').value;
        const time = document.getElementById('scheduleTime').value;

        if (!name || !phone || !service || !date || !time) {
            alert('Por favor, preencha todos os campos.');
            return;
        }

        const dateObj = new Date(date + 'T00:00:00');
        const formattedDate = dateObj.toLocaleDateString('pt-BR');

        const message =
            `👑 *NOVO AGENDAMENTO - CABEÇA BARBER SHOPER* %0A%0A` +
            `👤 *Nome:* ${name}%0A` +
            `📱 *Telefone:* ${phone}%0A` +
            `✂️ *Serviço:* ${service}%0A` +
            `📅 *Data:* ${formattedDate}%0A` +
            `🕐 *Horário:* ${time}%0A%0A` +
            `Quero confirmar meu horário! 🔥`;

        window.open(`https://wa.me/5513996286050?text=${message}`, '_blank');

        document.getElementById('successModal').classList.add('active');
        this.reset();

        // Mini confetti
        launchMiniConfetti();
    });

    function closeModal() {
        document.getElementById('successModal').classList.remove('active');
    }

    // ===== MINI CONFETTI =====
    function launchMiniConfetti() {
        const colors = ['#c9a84c', '#e8d28a', '#b33a2a', '#fff', '#f5f0eb'];
        for (let i = 0; i < 35; i++) {
            setTimeout(() => {
                const el = document.createElement('div');
                el.style.cssText = `
                        position: fixed;
                        width: ${6 + Math.random() * 12}px;
                        height: ${6 + Math.random() * 12}px;
                        background: ${colors[Math.floor(Math.random() * colors.length)]};
                        border-radius: ${Math.random() > 0.5 ? '50%' : '0'};
                        left: ${Math.random() * 100}%;
                        top: -10px;
                        pointer-events: none;
                        z-index: 9999;
                        box-shadow: 0 0 20px ${colors[Math.floor(Math.random() * colors.length)]};
                        animation: confettiFall ${2 + Math.random() * 2}s ease-in forwards;
                    `;
                document.body.appendChild(el);
                setTimeout(() => el.remove(), 4500);
            }, i * 60);
        }
    }

    // Adiciona keyframes para confetti
    const styleConfetti = document.createElement('style');
    styleConfetti.textContent = `
            @keyframes confettiFall {
                0% { transform: translateY(0) rotate(0deg); opacity: 1; }
                100% { transform: translateY(100vh) rotate(720deg); opacity: 0; }
            }
        `;
    document.head.appendChild(styleConfetti);

    // Fecha modal ao clicar fora
    document.getElementById('successModal').addEventListener('click', function(e) {
        if (e.target === this) closeModal();
    });
</script>

</body>
</html>
